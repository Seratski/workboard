# Generated by tests/browser/build-harness.js on every run: index.html with the Firebase
# CDN tags swapped for the offline stub. Never edit it, never commit it.
tests/browser/test.html

# Screenshots a suite may write while debugging.
tests/browser/*.png

# Nothing in this repository has dependencies. If node_modules appears, something went wrong.
node_modules/
